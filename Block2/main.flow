import MTaC/Block2/calculator;
import sys/system;


main() {
    str_exp : string = readUntil("\n");
    println(s2exp(str_exp));
    println(exp2s(s2exp(str_exp)));
    println(exp2s_polish(s2exp(str_exp)));
    println(calcExp(s2exp(str_exp)));   
}